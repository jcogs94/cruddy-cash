<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <link rel="stylesheet" href="/css/style.css">
    <title>CRUDdy Cash - Edit <%= budget.name %></title>
</head>
<body>
    <%- include('../partials/nav.ejs') %>
    <main>
        <h1><%= budget.name %></h1>
        <form action="/budgets/<%= budget._id %>?_method=put" method="post">
            <fieldset aria-labelledby="new-entry-legend">
                <legend name="new-entry-legend">Update Budget</legend>
                <div>
                    <label for="month">Month:</label>
                    <input type="month" name="month" value="<%= `${budget.year}-${budget.monthNumStr}` %>">
                </div>
                <button type="submit">Update</button>
            </fieldset>
        </form>
    </main>
</body>
</html>